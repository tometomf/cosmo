<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.cosmo.mapper.IchijiHozonMapper">

    <!-- ICHIJI_HOZON INSERT -->
    <insert id="insertIchijiHozon"
            parameterType="org.cosmo.domain.IchijiHozonDTO">

        <!-- PK 시퀀스 생성 -->
        <selectKey keyProperty="hozonUid"
                   resultType="int"
                   order="BEFORE">
            SELECT ICHIJI_HOZON_SEQ.NEXTVAL FROM DUAL
        </selectKey>

        INSERT INTO ICHIJI_HOZON (
            HOZON_UID,
            USER_UID,
            SHINSEI_KBN,
            SHOZOKU_CD,
            ALERT_NO,
            ALERT_SEQ,
            ACTION_NM,
            DATA,
            ADD_USER_ID,
            ADD_DATE,
            UPD_USER_ID,
            UPD_DATE
        ) VALUES (
            #{hozonUid,   jdbcType=INTEGER},
            #{userUid,    jdbcType=INTEGER},
            #{shinseiKbn, jdbcType=VARCHAR},
            #{shozokuCd,  jdbcType=VARCHAR},
            #{alertNo,    jdbcType=INTEGER},
            #{alertSeq,   jdbcType=INTEGER},
            #{actionNm,   jdbcType=VARCHAR},
            #{data,       jdbcType=BLOB},
            #{addUserId,  jdbcType=INTEGER},
            SYSTIMESTAMP,
            #{updUserId,  jdbcType=INTEGER},
            SYSTIMESTAMP
        )
    </insert>

</mapper>
